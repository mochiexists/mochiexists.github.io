# B:/ Start Up

This is mochiexists.com — a static site for Mochi, hosted on GitHub Pages.

- Read this, then `AGENTS.md`, then `.spec/FLOW.md`, then the active change file
- Static HTML/CSS/JS only — no build tools, no frameworks
- Deployed via GitHub Pages from `main` branch with CNAME `mochiexists.com`
- Refresh sod with `bash scripts/update-sod-report.sh` after tracked text-file changes

teams: none
merge: confirm
merge-target: main
push: confirm
